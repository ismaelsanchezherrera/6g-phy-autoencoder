# -*- coding: utf-8 -*-
"""05_evaluation_and_benchmarking.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1NZkRT92XpGG8UiFdeD8XO2h_-nLvo3Hg

"""

import sys
import torch
sys.path.append("..")

from src.models.autoencoder import EndToEndAutoencoder
from src.evaluate import plot_learned_constellation, evaluate_ber, theoretical_qpsk_ber, plot_ber_curves

device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
model = EndToEndAutoencoder(num_symbols=16, channel_dims=2, use_csi=False).to(device)
model.load_state_dict(torch.load("../models_checkpoints/autoencoder_awgn.pth", map_location=device))
print("Modelo cargado correctamente.")

plot_learned_constellation(model, title="Constelación Aprendida (M=16)", save_path="../docs/constellation_learned.png")

ebno_axis, ber_nn = evaluate_ber(model, channel_type="awgn", ebno_db_range=range(0, 12, 2))
ber_qpsk = theoretical_qpsk_ber(ebno_axis)
plot_ber_curves(ebno_axis, ber_nn, ber_qpsk, label="Autoencoder (16-Símbolos)", save_path="../docs/ber_performance_curve.png")